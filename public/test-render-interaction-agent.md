# Lattice 渲染与交互综合测试

## 标题二级

### 标题三级

这是一个段落，包含 **粗体**、*斜体*、~~删除线~~、`行内代码`、[外部链接](https://example.com) 与 ![图标](/icons/icon-192x192.png)。

引用与行内格式：> 这句引用里有 **粗体** 和 $E=mc^2$。

> 一级引用
> > 二级引用（嵌套）

- 无序列表项 A
- 无序列表项 B
  - 无序嵌套项 B.1（含 *斜体*）
  - 无序嵌套项 B.2（含 [链接](https://openai.com)）
1. 有序列表项 1
2. 有序列表项 2
   1. 有序嵌套项 2.1

```ts
function add(a: number, b: number): number {
  return a + b;
}
console.log(add(1, 2));
```

行内公式：$a^2 + b^2 = c^2$。

$$
\int_0^1 x^2 \, dx = \frac{1}{3}
$$

| 列 1 | 列 2 | 列 3 |
|:-----|:----:|-----:|
| **粗体** | *斜体* | ~~删除~~ |
| [链接](https://example.com) | $x+y$ | `code` |

末尾段落用于点击光标定位测试。
