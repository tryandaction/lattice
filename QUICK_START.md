# Lattice 快速上手指南

## 🚀 5 分钟快速开始

### 选择你的使用方式

#### 方式 1：在线使用（最快）⚡

1. 访问 **https://lattice-apq.pages.dev/**
2. 点击"打开文件夹"开始使用
3. 完成！

**优点**：无需下载，立即使用  
**缺点**：每次需要授权文件访问，无法记住工作目录

---

#### 方式 2：桌面应用（推荐）🖥️

1. **下载适合你系统的版本**：
   - Windows: [下载 NSIS 安装包](https://github.com/YOUR_USERNAME/lattice/releases/latest/download/Lattice_0.1.0_x64-setup.exe)
   - macOS: [下载 DMG 镜像](https://github.com/YOUR_USERNAME/lattice/releases/latest/download/Lattice_0.1.0_x64.dmg)
   - Linux: [下载 AppImage](https://github.com/YOUR_USERNAME/lattice/releases/latest/download/lattice_0.1.0_amd64.AppImage)

2. **安装并启动**：
   - Windows: 双击安装包，按向导完成安装
   - macOS: 打开 DMG，拖拽到 Applications 文件夹
   - Linux: 添加执行权限 `chmod +x *.AppImage`，然后运行

3. **设置默认文件夹**（可选）：
   - 点击右下角的设置按钮（⚙️）
   - 选择你的常用工作目录
   - 点击保存

4. **开始使用**！

**优点**：启动快、记住工作目录、完整文件权限  
**缺点**：需要下载 ~6-8 MB

---

## 📚 基本功能

### 1. 打开文件/文件夹

**网页版**：
- 点击左上角的"打开文件夹"按钮
- 选择你的工作目录
- 授权文件访问权限

**桌面版**：
- 如果设置了默认文件夹，启动时自动打开
- 否则点击"打开文件夹"按钮选择

### 2. 编辑 Markdown

1. 在文件浏览器中点击 `.md` 文件
2. 开始编辑
3. 支持：
   - 标准 Markdown 语法
   - 数学公式（LaTeX）
   - 代码高亮
   - 表格、列表等

### 3. 查看 PDF

1. 点击 `.pdf` 文件
2. 支持：
   - 文字高亮
   - 区域选择
   - 添加批注
   - 添加评论

### 4. 编辑代码

1. 点击代码文件（`.js`, `.py`, `.ts` 等）
2. 自动语法高亮
3. 支持多种编程语言

### 5. 数学公式

在 Markdown 中：
- 行内公式：`$E = mc^2$`
- 块级公式：
  ```
  $$
  \int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
  $$
  ```

或使用结构化编辑器：
- 按 `/` 创建分数
- 按 `Tab` 在占位符间导航

### 6. 运行 Python 代码

1. 打开 `.ipynb` 文件或创建代码块
2. 写入 Python 代码
3. 点击运行按钮
4. 查看输出结果

（首次运行会下载 Pyodide，约 20MB）

---

## ⌨️ 快捷键

### 通用
- `Ctrl+B` / `Cmd+B` - 切换侧边栏
- `Ctrl+S` / `Cmd+S` - 保存文件
- `Ctrl+O` / `Cmd+O` - 打开文件

### 桌面应用专属
- `Ctrl+,` / `Cmd+,` - 打开设置

### 编辑器
- `Ctrl+Z` / `Cmd+Z` - 撤销
- `Ctrl+Y` / `Cmd+Y` - 重做
- `Ctrl+F` / `Cmd+F` - 查找
- `Ctrl+H` / `Cmd+H` - 替换

---

## 🎯 常见使用场景

### 场景 1：写笔记

1. 打开你的笔记文件夹
2. 创建或打开 `.md` 文件
3. 使用 Markdown 语法写作
4. 插入数学公式、代码块、图片等
5. 自动保存

### 场景 2：阅读和批注 PDF

1. 打开 PDF 文件
2. 选择文字进行高亮
3. 添加批注和评论
4. 批注自动保存

### 场景 3：编写代码

1. 打开代码文件
2. 享受语法高亮
3. 编辑并保存
4. 在外部运行或测试

### 场景 4：数据分析（Jupyter Notebook）

1. 打开 `.ipynb` 文件
2. 编写 Python 代码
3. 运行单元格
4. 查看结果和可视化

### 场景 5：演示文稿预览

1. 打开 `.pptx` 文件
2. 预览幻灯片
3. 导出为图片或 PDF

---

## 💡 使用技巧

### 桌面应用技巧

1. **设置默认文件夹**：
   - 将常用工作目录设为默认
   - 每次启动自动打开，节省时间

2. **固定到任务栏/Dock**：
   - 方便快速启动
   - 无需每次从开始菜单找

3. **关闭浏览器**：
   - 使用桌面应用后可以关闭浏览器
   - 节省内存和 CPU

### 编辑技巧

1. **使用结构化数学编辑**：
   - 按 `/` 创建分数
   - 按 `^` 创建上标
   - 按 `_` 创建下标
   - 比直接写 LaTeX 更快

2. **拖拽文件**：
   - 直接拖拽图片到编辑器
   - 自动插入图片链接

3. **多窗格布局**：
   - 拖拽文件到不同位置
   - 同时查看多个文件

---

## 🔧 设置和配置

### 桌面应用设置

按 `Ctrl+,` 打开设置界面：

- **默认文件夹**：设置启动时自动打开的文件夹
- **上次打开的文件夹**：查看最近使用的文件夹

### 主题（即将推出）

- 浅色主题
- 深色主题
- 自动切换

---

## ❓ 常见问题

### Q: 网页版和桌面版有什么区别？

A: 主要区别：

| 特性 | 网页版 | 桌面版 |
|------|--------|--------|
| 启动速度 | 较慢 | 快速 |
| 文件访问 | 需要授权 | 完整权限 |
| 记住目录 | ❌ | ✅ |
| 离线使用 | 有限 | 完全支持 |

### Q: 如何更改默认文件夹？

A: 
1. 按 `Ctrl+,` 打开设置
2. 点击"选择文件夹"
3. 选择新的文件夹
4. 点击"保存"

### Q: 数据存储在哪里？

A: 
- **文件内容**：存储在你选择的本地文件夹中
- **应用设置**（桌面版）：
  - Windows: `%APPDATA%\com.lattice.editor\`
  - macOS: `~/Library/Application Support/com.lattice.editor/`
  - Linux: `~/.config/com.lattice.editor/`

### Q: 支持哪些文件格式？

A: 
- **编辑**：Markdown (`.md`), 代码文件
- **查看**：PDF, Jupyter Notebook (`.ipynb`), PowerPoint (`.pptx`), Word (`.docx`), 图片
- **运行**：Python (通过 Pyodide)

### Q: 如何卸载？

A: 
- **Windows**：控制面板 > 程序和功能 > 卸载 Lattice
- **macOS**：将 Lattice.app 拖到废纸篓
- **Linux**：
  - AppImage: 直接删除文件
  - DEB: `sudo apt remove lattice`

---

## 📖 进一步学习

- [完整文档](./README.md)
- [桌面功能详解](./docs/DESKTOP_FEATURES.md)
- [安装指南](./INSTALLATION.md)
- [架构设计](./docs/ARCHITECTURE.md)

---

## 🤝 获取帮助

- [GitHub Issues](https://github.com/YOUR_USERNAME/lattice/issues)
- [讨论区](https://github.com/YOUR_USERNAME/lattice/discussions)
- 邮件：your-email@example.com

---

**开始你的高效编辑之旅！** 🚀
